apiVersion: v1
kind: Service
metadata:
  name: rate-limiter-metrics
  namespace: rate-limiter
  labels:
    app: rate-limiter
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app: rate-limiter
  ports:
  - name: metrics
    port: 8080
    targetPort: 8080
    protocol: TCP
